#!/usr/bin/env bash

binary="$HOME/.dotfiles/build/gen_compile_flags"
source_c="$HOME/.dotfiles/src/gen_compile_flags.c"

if [[ ! -f "$binary" || "$source_c" -nt "$binary" ]]; then
	cc -Wall -Werror -Wextra -o "$binary" "$source_c" || { echo "Compilation failed"; exit 1; }
fi

"$binary"
