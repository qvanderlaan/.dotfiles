#!/usr/bin/env bash
source "$HOME/.dotfiles/lib/colors.sh"

warning "Are you sure you want to reset all changes? (y/n)"
read response
if [[ "$response" != "y" && "$response" != "Y" ]]; then
	error "Changes have not been reset."
	exit 0
fi
git reset --hard
success "Changes have been reset."
